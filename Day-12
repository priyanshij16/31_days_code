 qs : Construct Binary Tree from Inorder and Preorder

if(s>e) return NULL;

make one new node insert krao root 
 for(i=s<e)
{
 if( inorder[i]== root->key)
 index=i;
break;
}
root->left= fun(s,index-1)
 root->right= fun(e,index+1)
 return root;
